# version: 2.1

# setup: true 

# orbs:
#   # slack: circleci/slack@3.4.2
#   path-filtering: circleci/path-filtering@0.0.2

# workflows:
#   node-app:
#     jobs:
#       - path-filtering/filter:
#           mapping: |
#             calculator/.* run-calculator true
#             helloworld/.* run-helloworld true
#           base-revision: main
#           config-path: .circleci/workflows.yml

version: 2.1

executors:
  node:
    docker:
      - image: cimg/node:16.5.0

jobs:
  npm-install:
    executor: node
    working_directory: ~/circleci-experiments/
    steps:
      - checkout:
          path: ~/circleci-experiments
      - run:
          name: Clean Install Dependencies
          command: |
            cd calculator && npm i && echo "changing dir to helloword";\
            cd ../helloworld && npm i
  
workflows:
  test-app:
    jobs:
      - npm-install:
          name: calculator install
